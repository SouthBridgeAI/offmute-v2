Voice Note:

Okay, so now we are getting into reviewing the code. At least now that everything is functional, I've been able to get reasonable outputs. Right, I'm just going to look at this if I want to. Just copy an automated review. that's also what just kind of looking at on sort of small things to fix here and there. That review was done by Opus, right, in all cases. Something to look at is also our test files. If they happen to be committed, we must need to clear up. (soft music) Okay, so for human viewer, where do we want to start? Okay, types, data model. Okay, let's start from SRC types. Okay. I'm word, confidence, speaker label, that's pretty good. Okay, that's not a term. ARSR-Arturance is not a bad way to put those things together. You get grouped speaker labels, but then your timed words also have a speaker label. So far in the typing, I'm seeing appropriate use of optional, but the weird thing is in some cases, like, okay. in ARSR utterance is not optional. Perfect. Words is optional. Maybe we'll sort of figure out why that is. Some things you're looking for here would be, whether optional is being used appropriately, if generics were needed and are used well. So, as a result that you're normalizing, you've got your utterances and your words. Okay. That is slightly confusing. I'm trying to figure out why you need words and utterances when utterances have words inside of them. That's not explained when I'm looking at this. But granted, I'm on file one. LLM line makes sense. Tone-- I'd be very interested in how you parse that out. how that sort of pass-through engine works and parse things out. It'd be the primary questions here. (audience member mumbles) Timing source makes sense. Good solid name types overall. This is metadata transcript. Organization goes bottom up on the type file, which is honestly pretty nice. I usually go top-down, but bottom-up also works. In some ways it's more readable. But I'm always wondering how these fit into the larger scale of things. Okay, so types.ts looks pretty good. Those two things that I mentioned are the ones not necessarily to fix yet, but to explain for me to understand, right? And then we've got the align.ts. Ooh, okay. Okay, so normalization makes sense, split into word tokens also makes sense. aligned pair. Neelman-Warnsch is not a terrible choice of algorithm for this kind of thing. Although the Opus Automated Review does mention that you could have done banding Hershberg's sort of optimizations, which we didn't. (keyboard clacking) In 32 is nice, you and Terry is nice. I don't know why there's a back pointer. Oh yeah, so the back pointer here is, oh okay, it's being explained, fair. That's, yeah, the comments are kind of where I expect. The needleman one thing I think is, links would be helpful, maybe line by line explanations of what's happening might also have been helpful, but given that this is a core thing, and you expect this to be maintainable by almost anyone coming in, you want these things to have a bit more explanation. (soft music) Overall, a line looks pretty good to me. that I've like mentally stepped through every single line, but the line looks pretty good to me. Okay. (keyboard clicking) Okay, wordless segments. So we actually haven't done a- OK. We are using quite a bit of memory here. It's not bad because it's still not a lot. Then we have chunk calculation. Okay, so this is where we picked up the formats and then the parsing. Interesting, okay. I want to know how successful that would be and how many versions we went through. So something I'm curious about is where I can see that in the intermediates. So I'm going to go dig into the intermediates as well to see what the raw LLM output looked like and then what the parsed LLM output looked like. This is in parsed, IRISE, or TS. (keyboard clicking) And what is build voice hint? What does that do exactly? Yeah what does line 46 here do? SP equals map.round. That's really interesting. templating into the string with a percentage number? Oh, K. Oh, because, oh, you're doing that. Okay. So what we kind of want in this case is we want to get the actual LLM calls. Like every single one of them, if possible. (student speaking) Got it, okay, that makes sense. So Gemini Flash latest, if you can't find it, output tokens, actually the newer ones have longer output tokens, so maybe that's better, but kinda argue with the fact that it's just working for the time being. But I would have loved to see with sort of the text generator class. I'm just going to go there really quick. So I mean, identify.ts, by the way. interesting so it depends on who is calling that okay we pass it sort of Gemini which is a Gemini client Okay, there's a German editor. Okay, interesting. Honestly, not against us. This is a pretty reasonable wrapper on everything. Okay, not too bad. I can say not too bad. Okay, so let's go back to identify speakers. So we should be able to hook in there and then just log, just add an option and then just log every single LLM call going through, right, just so we can see which calls are made. Just so I can come in and inspect the prompts and what happens. And for JSON, I'm not exactly sure. Okay, let's see what the responses that come back are. How are we parsing the JSON? We are... Okay, interesting. We don't ask it to respond in markdown code blocks. That might be better to ask it to respond markdown code blocks and then inside we can get JSON. Awkward work, okay. In terms of the JSON parsing. But is there not like a JSON mode that we can use? I forget if there was something for us to check out. You know, we've got the formatterspeakers.ts and stuff. of ok also the alias is basically alias is not necessarily the term I mean speaker shut yes alias is not necessarily the term because you are going from unidentified to identified basically that. Aleses would be better if you had different spelling in different groups. Unless we are doing that, if we're not doing that, then Aleses isn't the right term, but that's again, very forgivable. SRT is so far seen clean. Format markdown group by speaker makes sense. time parsing pretty solid. We actually in our tests checking to see if we're if the SRT is parsable? No, our tests are based on the specific files. Our tests ideally would have had- Yeah, I see the unit test. But some really useful behavioral tests would have been to test if we always produce a valid SRT, if we always produce valid markdown, that kind of thing, to test our formators. Okay, now we get the pipeline. All right, transcribe. We've got our main primary transcribe function here. Options were, okay, we're pulling it out that way. Pre-process audio. So we're using the intermediate folder itself as state. It's 'cause we're not passing it around. What might have been nicer is to pass the data around, load and pass the data around and have checks at each point, in case you run into an error or a bug. Because a lot of these things are big enough to keep in memory, to sort of keep passing. In case something happens to the intermediate folder. The intermediate folder-- this is interesting. (student speaking) Yeah, that makes sense for the most part. I would have liked to have seen a little bit more try catch and sort of exception catching and throwing back to the user so we know what failed where but that's okay. This is in sort of pipeline.ts. We've got the browser pipeline. (typing) And then you get Gemini, probably get simply I not too bad. Fafnpeg, yep. Fafnpeg Wasm, okay. Ok, wonderful there is a browser indexer estimate. I can check the browser thing. Okay, wonderful. Examples browser. Oh, perfect. Okay. That is a really nice touch that you can just throw in a browser tester. Ah, interesting. Workers can't be accessed from here. That's an interesting thing. There's a little error I'm just finding out if that's us or not. Okay, our packages are pretty good. Quality-wise, this is also pretty good. So this is my sort of like review of the code. Once we're done reviewing the actual browser path itself and just testing it, we're good. Okay, so for the worker, I think you need to blobify it. Might be one way to sort of test it, instead of make sure that that works. I mean now also review the readme. OK CLI is reasonably easy to understand. Library is reasonably easy to understand. the on progress callback is pretty good. A boat controller would have been nice to have, but that's okay. .
