Okay, so we are now going through and reviewing the version of off right off mute version two we're gonna go through the code we're gonna like try and understand it and I'm gonna review it as a as a senior depth let's say Okay, we do have some length issues that are just sitting around which is not nice. They're not super bad or anything, but it's okay. But we did build in the lint thing, so that's a big point there. Okay, very different way of typing things and it is structured. Honestly, pretty well. Reasonably simple type section, not too much there. Okay, we have a thing for resolving the security model, resolving the keys themselves. We do have model presets, which we didn't in the other version. This is kind of like OffMirror. It's a wonderful thing to have learned from OffMirror. Okay. Yeah. So, IOS+ ModifyTime is a better way to build a content hash. It's something that we do in one of the dependencies as well. So this is significantly better in terms of sticking to the conventions from the background. This code is significantly more readable to me. What does that mean? Instead of dot slash relative address the intermediate folder to what? So that's the default intermediate. This might be where our bugs are coming from because we, when we run it wherever, you are doing a./informity instead of anchoring to either where the file is from, where the program is, or some temp directory or var life kind of thing. That's a bit of a boo boo. it shouldn't happen. Okay. So that was config.ts, just me going through. Normalize, okay. Okay, tokenization looks clean. It just is far more readable than the other version, just use a big chunky reg X. So here we do use reg Xs, But to me, it's personally easy to read. I'm not sure about performance, so it's something to compare. And we do have an edit distance algorithm that we wrote ourselves. We also use needleman onech, but in little men once we're using edit distance here. We don't mention banding, but this is, okay, a bit better explained honestly. There's less explanation, but in some ways it's weird. This is way more readable to me than what I'm looking at. Then we've got a liner. I like that the files are grouped a little bit better into their own folders. If you're going to do that, otherwise you just get like 50 files, right? So this way you can see align core as a module, X, Y, Z. Okay, interesting. So we want to compare the alignment later on, but we won't do that yet. so far I like it Yeah. Okay, that makes sense. We do have providers. I don't know, set up. And we're actually using the Gemini. Okay, and we're getting sort of the -- okay. And what we also want to do is, since we have that map, we want to add an option to log every single LLM call and response so I can go in and validate it. Yeah, but mostly everything looks decent. It's just the, It's just the sort of relative interest intermediate folder that might be causing most of the issues. We also have these parts.push. The way we create prompts, which is multi-line strings, is a bit all over the place. Um, four makes, yeah. (typing) Audio, we've got FFM peg. Okay, so the browser constraints here is that we just don't have FFMPEG straight up. So that's a pretty big comparison and issue. I mean, we don't need to add it right now, but it's something to mark down. But skipping FFMPEG kind of means that we have skipped the complexity of just doing that. Right? And we've hard coded Gemini 2.5 flash as our base model. These things would have been better as constants.
